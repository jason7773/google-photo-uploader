Metadata-Version: 2.4
Name: gp-p1-mig
Version: 0.1.0
Summary: Google Photos Takeout migration CLI for Pixel 1 relay upload
Author: gp-p1-mig
Requires-Python: >=3.11
Description-Content-Type: text/markdown
Requires-Dist: typer>=0.12
Requires-Dist: rich>=13.7
Requires-Dist: flask>=3.0
Requires-Dist: flask-socketio>=5.3
Provides-Extra: dev
Requires-Dist: pytest>=7; extra == "dev"
Requires-Dist: pillow; extra == "dev"

# gp-p1-mig: Google Photos Takeout 遷移助手

![License](https://img.shields.io/badge/license-MIT-blue.svg)
![Python](https://img.shields.io/badge/python-3.11+-blue.svg)
![Status](https://img.shields.io/badge/status-stable-green.svg)

**gp-p1-mig** 是一個專為 Google Photos 遷移設計的自動化工具。它的核心目標是解決使用 Google Takeout 匯出的照片/影片遺失 Metadata（拍攝時間、GPS）的問題，並透過自動化流程將修復好的檔案批次推送到 Pixel 1 裝置，以享受 Google Photos 的「原畫質無限儲存」福利。

## ✨ 主要功能

- **自動化 Metadata 修復**：
  - 自動解析 Google Takeout 的 JSON sidecar 檔案。
  - 將正確的「拍攝時間」和「GPS 座標」寫回照片 (Exif) 與影片 (QuickTime/FFmpeg) 檔案中。
  - 支援處理 PNG 偽裝成 JPEG 的特殊情況。
- **支援廣泛格式**：
  - **圖片**：JPG, HEIC, PNG, GIF, WEBP, BMP, TIFF
  - **RAW**：DNG, CR2, CR3, NEF, ARW, ORF, RAF 等
  - **影片**：MP4, MOV, AVI, MKV, 3GP, MPG, WMV 等
- **智慧去重**：
  - 使用 SHA-256 雜湊值比對，確保不會重複處理相同的檔案（即使檔名不同）。
  - 自動將重複檔案移至垃圾桶，節省空間。
- **Web UI 視覺化管理**：
  - 提供現代化的 Web 介面，透過瀏覽器即可管理整個遷移流程。
  - 儀表板即時顯示處理進度、成功率與工具狀態。
- **批次處理與驗證**：
  - 自動將大量檔案打包成合適大小的批次 (Batch)。
  - 透過 ADB 自動推送到 Pixel 裝置。
  - 自動抽樣驗證檔案是否正確寫入裝置，確保資料安全。

## 🛠️ 安裝需求

請確保您的系統已安裝以下工具：

1. **Python 3.11+**
2. **ExifTool**：用於讀寫圖片 Metadata。
   - [下載 ExifTool](https://exiftool.org/) 並放入 `tools/` 目錄或加入系統 PATH。
3. **FFmpeg**：用於寫入影片 Metadata。
   - [下載 FFmpeg](https://ffmpeg.org/download.html) 並放入 `tools/` 目錄或加入系統 PATH。
4. **ADB (Android Debug Bridge)**：用於連接 Pixel 裝置。
   - [下載 SDK Platform-Tools](https://developer.android.com/studio/releases/platform-tools) 並放入 `tools/` 目錄或加入系統 PATH。

### 安裝步驟

```bash
# 複製專案
git clone https://github.com/jason7773/gp-p1-mig.git
cd gp-p1-mig

# 安裝依賴 (建議使用虛擬環境)
pip install -e .
```

## 🚀 使用指南

本工具提供 **Web UI** (推薦) 與 **CLI** 兩種介面。

### 方法一：Web UI (推薦)

啟動 Web 介面後，瀏覽器會自動開啟 `http://127.0.0.1:5000`。

```bash
gp-p1-mig-ui
```

在網頁上，您可以依照步驟指引完成：
1. **設定路徑**：指定 Takeout 解壓目錄與工作目錄。
2. **Ingest**：匯入 Takeout 檔案，系統會自動去重並建立索引。
3. **Reconcile**：比對 JSON sidecar，找出正確的 Metadata。
4. **Patch**：執行 Metadata 寫入作業。
5. **Batch**：建立上傳批次。
6. **Push**：連接 Pixel 1，一鍵推送到手機。
7. **Verify & Purge**：驗證成功後清理已上傳檔案。

### 方法二：CLI (進階)

如果您偏好命令行操作，可以使用 `gp-p1-mig` 指令：

```bash
# 1. 初始化資料庫
gp-p1-mig init

# 2. 匯入 Takeout Zip (支援多個 Zip)
gp-p1-mig ingest D:\Takeout\takeout-001.zip

# 3. 比對 Sidecar 資料
gp-p1-mig reconcile

# 4. 寫入 Metadata (這是最耗時的步驟)
gp-p1-mig patch

# 5. 建立批次 (例如每批 2GB 或 500 個檔案)
gp-p1-mig make-batch --max-bytes 2147483648 --max-files 500

# 6. 推送到裝置
gp-p1-mig push <BATCH_ID> --device-path /sdcard/DCIM/Camera

# 7. 清理已完成的批次
gp-p1-mig purge <BATCH_ID>
```

## 📂 目錄結構

```text
gp-p1-mig/
├── pyproject.toml       # 專案設定與依賴
├── README.md            # 說明文件
├── sql/                 # 資料庫 Schema
├── scripts/             # 實用腳本 (Smoke Test, Build EXE)
├── tools/               # 放置外部工具 (adb, ffmpeg, exiftool)
└── src/
    └── gp_p1_mig/
        ├── db.py        # 資料庫操作
        ├── workflow.py  # 核心邏輯
        ├── web_ui.py    # Web UI 啟動入口
        └── web/         # Web 應用原始碼 (Flask + Jinja2)
```

## ⚠️ 免責聲明

本工具涉及檔案修改與刪除操作，雖然開發過程中已盡力確保資料安全（例如：Patch 操作是針對副本進行，原始檔保留；Purge 操作僅在驗證後執行），但作者不對任何因使用本工具而導致的資料遺失負責。

**強烈建議：**
1. 在執行任何「寫入」或「刪除」操作前，請確保您有額外的資料備份。
2. 先使用少量的範例檔案進行測試 (Smoke Test)，確認流程符合預期。

## 🤝 貢獻

歡迎提交 Issue 或 Pull Request 來改進這個專案！

---
Built with ❤️ for the Pixel community.
